
  Legend: x=column
          y=row
          a=asci character
          c=ansi color    
          n=number of cells
          t=text string
          s=state (on/off) 
 
            pokeCell(x,y,a) — poke character(s) to a cell (fast)
          pokeChar(x,y,a,n) — poke character(s) to cells (slow)
  
            pokeFG(x,y,c,n) — set foreground color
            pokeFG(x,y,c,n) — set background color
   pokeColor(x,y,fg, bg, n) — set foreground and background color 
       pokeBold(x, y, s, n) — set bold attribute       
       pokeInverse(x,y,s,n) — set inverse attribute

              pokeCursor(t) — poke text at cursor's position          
            pokeText(x,y,t) — poke text at x,y (0,0 if not supplied)
  
                pokeInput() — poke input line at cursor position
              pokeSelect(s) — set inverse attribute for text selection
                  
           pokeRefresh(x,y) — update DOM cell, if x/y omited refresh entire DOM
    pokeRefreshRow(y, x, n) — update row of DOM cells 
             
         pokeRefreshRow — (used by pokeInverse span path; referenced)
    pokeAttrBit / window._setAttrBit — helpers to set ATTR bitfield
       pokeAttr   
      
       (peekAttr/pokeAttrBit) / window.peekAttr — ATTR helpers (pokeAttrBit is used)
     setAttrBit — low-level ATTR bit setter used by pokeAttrBit/pokeInverse
 
