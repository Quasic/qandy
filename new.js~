
run="new.js"; // name of script, reset to "qandy.js" on exit

function input(l) {
  // required to handle l (input line)
}

function keydown(k) {
  // required to process k (onkeydown)
}

let menuColor="\x1b[37;44m";
let textColor="\x1b[0m";

menu=menuColor+" File Edit"; cls(); draw(menu); menu=' '.repeat(32-cursorX); draw(menu);

text=textColor+"The quick brown fox jumped over the lazy dogs.";
draw(text);

